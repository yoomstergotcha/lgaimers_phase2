experiment:
  id: exp_001_reltrend
  description: "Example child experiment; change config, not pipeline filename"
  parent: exp_000_v4_repro

runner:
  implementation: legacy_v4
  device: auto

features:
  temporal_history_scope: all

training:
  sample_weight_mode: none
  old_f_weight: 0.25
  dev_2023_weight: 2.0
  catboost_locked_candidates: 3
  seeds: [41, 42, 43]

transformer:
  d_model: 64
  n_heads: 4
  n_layers: 2
  ffn_dim: 128
  dropout: 0.15
  token_dropout: 0.10
  batch_size: 512
  lr: 0.001
  weight_decay: 0.0001
  max_epochs: 60
